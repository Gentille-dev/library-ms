{
  "name": "sequelize_library",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "make-models": " npx sequelize-cli model:generate --name Book --attributes bookAuthor:string,bookTitle:string,bookGenre:string,bookBorrower:string,bookOwner:string,ISBN:integer",
    "migrate": "npx sequelize-cli db:migrate",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pg": "^8.10.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.29.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.21",
    "sequelize-cli": "^6.6.0"
  }
}
